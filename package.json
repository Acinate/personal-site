{
  "scripts": {
    "build": "sudo npm run build --prefix ./frontend && sudo npm run build --prefix ./backend",
    "compose": "sudo npm run build && sudo docker-compose build",
    "start": "sudo docker-compose up -d"
  }
}
